
version: 2

models:
  - name: orders_cleaned
    description: "A cleaned version of the orders table"
    columns:
      - name: order_id
        description: "The primary key for this table"
        data_tests:
          - unique
          - not_null