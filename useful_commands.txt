commands

----------------
** venv **
python -m venv .venv
source .venv/bin/activate

-------
brew install postgresql
brew services start postgresql
pip install dbt-postgres
sudo -u postgres psql
psql postgres
psql -d dbt_local CONNEXT TO DB
brew services list
--------------
***IN PSQL***
CREATE DATABASE nicogreen;
psql -d nicogreen
\du LIST USERS
CREATE USER dbt_user WITH PASSWORD 'dbt_password';
\q QUIT
\l LIST DATABASES
psql -U dbt_user -d cat_corp (LOGIN TO DB)
\dt *.* LIST ALL TABLES

------------
** dbt **
dbt --version
dbt init my_project
cd my_project

dbt run --select orders_cleaned (RUN A MODEL)
--------------
** airflow **
airflow db init
password 2ufzehbtAdWqYvf9
airflow standalone



having airflow and dbt installed in the same venv caused dependency issues. 
must delete dbt venv and reinstall dependencies. no biggie, just a venv.

trying again in the morning.